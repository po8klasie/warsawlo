import React from 'react'
import AppLayout from 'components/app/Layout'
import styled from '@emotion/styled'
import { Link } from 'gatsby'

export default props => (
    <AppLayout location={props.location}>
     <h1>Mój wybór</h1>
    </AppLayout>
   
)